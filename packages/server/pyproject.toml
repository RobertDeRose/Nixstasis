[project]
name = "server"
dynamic = ["version"]
description = "Monitoring and Remote Access to SFERO deployments"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "fastapi>=0.121.2",
    "reflex[db]~=0.8.19",
    "reflex-azure-auth>=0.1.1",
    "starlette-admin>=0.15.1",
]

[tool.uv]
package = false
fork-strategy = "fewest"
