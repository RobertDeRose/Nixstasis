defmodule Nixstasis.Repo.Migrations.CommandPolicyDeliveryResults do
  @moduledoc """
  Updates resources based on their most recent snapshots.

  This file was autogenerated with `mix ash_postgres.generate_migrations`
  """

  use Ecto.Migration

  def up do
    create table(:command_policy_delivery_results, primary_key: false) do
      add :id, :uuid, null: false, default: fragment("gen_random_uuid()"), primary_key: true
      add :status, :text, null: false
      add :command_ref, :text
      add :client_payload, :map, null: false, default: %{}
      add :failure_reason, :text
      add :reported_at, :utc_datetime, null: false, default: fragment("(now() AT TIME ZONE 'utc')")
      add :inserted_at, :utc_datetime_usec, null: false, default: fragment("(now() AT TIME ZONE 'utc')")

      add :assignment_id,
          references(:command_policy_assignments,
            column: :id,
            name: "command_policy_delivery_results_assignment_id_fkey",
            type: :uuid,
            prefix: "public",
            on_delete: :delete_all
          ),
          null: false

      add :pending_command_id,
          references(:pending_commands,
            column: :id,
            name: "command_policy_delivery_results_pending_command_id_fkey",
            type: :uuid,
            prefix: "public",
            on_delete: :nilify_all
          )
    end

    create index(:command_policy_delivery_results, [:reported_at])

    create index(:command_policy_delivery_results, [:status])

    create index(:command_policy_delivery_results, [:pending_command_id])

    create index(:command_policy_delivery_results, [:assignment_id])
  end

  def down do
    drop constraint(:command_policy_delivery_results, "command_policy_delivery_results_assignment_id_fkey")

    drop constraint(:command_policy_delivery_results, "command_policy_delivery_results_pending_command_id_fkey")

    drop_if_exists index(:command_policy_delivery_results, [:assignment_id])

    drop_if_exists index(:command_policy_delivery_results, [:pending_command_id])

    drop_if_exists index(:command_policy_delivery_results, [:status])

    drop_if_exists index(:command_policy_delivery_results, [:reported_at])

    drop table(:command_policy_delivery_results)
  end
end
