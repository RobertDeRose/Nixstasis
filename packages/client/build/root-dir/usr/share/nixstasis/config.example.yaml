api:
  url: "https://nixstasis.example.com"

poll:
  interval: 30s

scripts:
  dir: "/usr/libexec/nixstasis/scripts"

frp:
  auth_token: "replace-me-with-the-frps-auth-token"

log:
  level: "info"
  format: "text"
